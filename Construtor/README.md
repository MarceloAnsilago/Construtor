# Construtor

Structure:
- `Construtor.mq5` at the repo root is the thin entry point.
- `Construtor/ConstrutorDialog.mqh` holds the constructor implementation.

This keeps the project easy to extend and easier to publish in GitHub as a small module instead of one monolithic EA file.
